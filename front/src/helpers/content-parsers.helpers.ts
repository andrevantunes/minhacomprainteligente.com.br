export { dateToBrTimezone, dateBrFull, dateBr, hourBr } from "@/helpers/date.helper";

export const not = (value: boolean) => !value;

export const toBoolean = (value: any) => !!value;

export const uppercase = (value: string) => value.toUpperCase();
export const lowercase = (value: string) => value.toLowerCase();
